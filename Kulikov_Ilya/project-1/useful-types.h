#ifndef USEFUL_TYPES_h
#define USEFUL_TYPES_h

enum State { Fail, Success };

using String = const char *;
using Integer = unsigned long long int;
using Percent = double;

using RUB_Kopecks = Integer;

using Kilometers = Integer;
using SquareMeters = Integer;
using Months = Integer;
using Milliliters = Integer;

#endif
